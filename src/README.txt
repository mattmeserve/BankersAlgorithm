BankerLab by Matthew Meserve

For compilation and running, type

javac Main.java

java Main input.txt (or whatever the input file’s name is)

NOTE: cannot have more than 5 resource types: will return an error if so
